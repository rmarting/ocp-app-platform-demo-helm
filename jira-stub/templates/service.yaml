---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "jira-stub.name" . }}
  labels:
    {{- include "jira-stub.labels" . | nindent 4 }}
spec:
  ports:
    - name: http
      port: {{ .Values.port }}
      targetPort: {{ .Values.port }}
  selector:
    name: {{ include "jira-stub.name" . }}
