apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "tpa-prerequisites.name" . }}
  labels:
    {{- include "tpa-prerequisites.labels" . | nindent 4 }}
    app.openshift.io/runtime: postgresql
  annotations:
    {{- include "tpa-prerequisites.argocd-syncwave" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "tpa-prerequisites.selectorLabels" . | nindent 6 }}
  strategy:
    type: Recreate
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "tpa-prerequisites.selectorLabels" . | nindent 8 }}
    spec:
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      serviceAccountName: {{ include "tpa-prerequisites.serviceAccountName" . }}
      containers:
        - name: postgresql
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: postgresql
              containerPort: {{ .Values.service.containerPort }}
              protocol: TCP
          livenessProbe:
            {{- toYaml .Values.livenessProbe | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.readinessProbe | nindent 12 }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          env:
            - name: POSTGRESQL_USER
              valueFrom:
                secretKeyRef:
                  key: database-user
                  name: {{ include "tpa-prerequisites.name" . }}
            - name: POSTGRESQL_PASSWORD
              valueFrom:
                secretKeyRef:
                  key: database-password
                  name: {{ include "tpa-prerequisites.name" . }}
            - name: POSTGRESQL_DATABASE
              valueFrom:
                secretKeyRef:
                  key: database-name
                  name: {{ include "tpa-prerequisites.name" . }}
          volumeMounts:
            - name: "{{ include "tpa-prerequisites.name" . }}-start"
              mountPath: "/opt/app-root/src/postgresql-start"
          {{- if eq .Values.persistence.mode "persistent" }}
            - name: "{{ include "tpa-prerequisites.name" . }}-data"
              mountPath: "/var/lib/pgsql/data"
          {{- end }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
      volumes:
        - name: "{{ include "tpa-prerequisites.name" . }}-start"
          configMap:
            name: {{ include "tpa-prerequisites.name" . }}-start
      {{- if eq .Values.persistence.mode "persistent" }}
        - name: "{{ include "tpa-prerequisites.name" . }}-data"
          persistentVolumeClaim:
            claimName: {{ include "tpa-prerequisites.name" . }}
      {{- end }}
